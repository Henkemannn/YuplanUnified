from collections.abc import Hashable, Mapping
from typing import Any


def is_ref(item: Mapping[Hashable, Any] | None) -> bool:
    return isinstance(item, dict) and "$ref" in item and item["$ref"].__hash__
